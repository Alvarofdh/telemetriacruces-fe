# Configuración de npm para el proyecto

# Usar cache de npm para builds más rápidos
cache=/tmp/npm-cache

# Guardar versiones exactas en package.json
save-exact=true

# No instalar dependencias opcionales por defecto
optional=false

# Nivel de log
loglevel=warn

# Usar lockfile version 2
lockfile-version=2

